gaApp.constant('appSettings', {
  title: 'GA Graduates',
  version: '1.0',
  createdBy: 'Lisa Plesko'
});

// To use, pass through controller to make available to scope
// $scope.appSettings = appSettings;

// In view
// {{ appSettings.title }}
