// describe('Unit: StudentsCtrl', function() {
//   beforeEach(module('gaApp'));

//   var ctrl, scope;

//   // inject the $controller and $rootScope services
//   beforeEach(inject(function($controller,$rootScope) {
//     // Create a new scope that's a child of the $rootScope
//     scope = $rootScope.$new();
//     ctrl = $controller('StudentsCtrl', {
//       $scope: scope
//     });
//   }));

//   it('should create $scope.students when calling getStudents',
//     function() {
//       expect(scope.students).toEqual([]);
//       scope.getStudents();
//       expect(scope.students).toEqual("Hello Ari");
//   });
// })
